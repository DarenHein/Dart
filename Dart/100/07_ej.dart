// calculando la potencia de un numero 
/**
 * todo para manejar el pow neceistamos dos cosas 
 * todo dart:mart 
 * todo y las nuevas variables llamadas num que pueden almacenar int y double como en puthon 
 */
import 'dart:math';

void main (){
  // ocuaperemos la funcion pow de dart que viene incluida 
  num bas =  2 ; 
  num exponente = 4 ; 
  funcion (2,4); 
}
void funcion (num bas , num exponente){
  num resultado =pow(bas,exponente); 
  print(resultado);
}
